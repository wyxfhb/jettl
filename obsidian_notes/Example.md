Actor:

Enum: msg, button
Send 5 messages to self, with Msg enum as input 1 second delay (append to array)
Process front panel: show array of 10 enum elements

Button: button enum, append to array

---

public API for an instrument already available
jettl to wraps this functionality

---

