incorporate the ping message and the debug / base debug libraries as intermediate actor.

At runtime, 
messages can be inspected in 'Msg Inspect.vi' and timestamped to show the system while it is executing.